--- Resources/app/administration/build/plugins.vite.ts.patch.orig
+++ Resources/app/administration/build/plugins.vite.ts.patch
@@ -219,7 +219,7 @@
                 // For plugins
                 server = await createServer({
                     ...getBaseConfig(extension),
-                    server: { port },
+                    server: { port, host: true },
                 });

                 extensionInfoDebug(colors.green(`# Plugin "${extension.name}": Injected successfully`));
